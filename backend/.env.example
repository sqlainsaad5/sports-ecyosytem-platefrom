# Copy to .env — never commit real secrets
MONGODB_URI=mongodb://127.0.0.1:27017/sports-ecosystem
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
ADMIN_PASSWORD=change-admin-login
ADMIN_EMAIL=admin@local.test

# Outbound email is not used — notifications are in-app only (see utils/notify.js).

# Stripe (optional). If STRIPE_SECRET_KEY is unset, payments stay in mock mode for local dev/tests.
# Use test keys from https://dashboard.stripe.com/test/apikeys — never commit live keys.
STRIPE_SECRET_KEY=
# Frontend (Vite): copy publishable key only — prefix pk_test_ or pk_live_
VITE_STRIPE_PUBLISHABLE_KEY=
