# Optional: override API base (defaults to same-origin /api via Vite proxy in dev)
# VITE_API_URL=http://localhost:5000/api

# Stripe Payment Element (test publishable key pk_test_... from Stripe Dashboard)
# VITE_STRIPE_PUBLISHABLE_KEY=

# Dev: Vite serves HTTPS on https://localhost:5173 (self-signed) when Node is not 22.21.0.
# Node 22.21.0 falls back to HTTP (Node HTTPS+HMR bug; upgrade to 22.21.1+). See nodejs/node#60336.
